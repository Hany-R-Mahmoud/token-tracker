# Quickstart: Analytics, Sharing, And Theme Parity

Required checks:

- `npm run typecheck`
- `npm run build`
- verify 7/30-day controls affect analytics views correctly
- verify heatmap renders for sparse and dense data
- verify cache-efficiency surfaces are honest
- verify dark mode and theme persistence
- verify clipboard/screenshot flows work
- verify leaderboard/social remains opt-in
