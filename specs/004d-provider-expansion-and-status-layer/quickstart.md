# Quickstart: Provider Expansion And Status Layer

Required checks:

- `npm run typecheck`
- `npm run build`
- verify each newly claimed provider on real or clearly validated local inputs
- verify unsupported providers remain explicit
- verify provider status states render correctly
- verify incident/degraded signals do not fabricate data
