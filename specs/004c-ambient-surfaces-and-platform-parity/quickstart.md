# Quickstart: Ambient Surfaces And Platform Parity

Required checks:

- `npm run typecheck`
- `npm run build`
- verify compact mode variants work and are understandable
- verify merged/minimal mode behaves correctly
- verify widget surface works or blocker is documented with evidence
- verify menu bar remains stable under live updates
