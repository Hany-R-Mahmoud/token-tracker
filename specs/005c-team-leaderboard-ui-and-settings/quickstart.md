# Quickstart: Team Leaderboard UI And Settings

Required checks:

- leaderboard page loads for a team member
- period switching works
- row click opens correct drawer data
- "My Rank" card appears only when opted in
- anonymous/opted-out/insufficient-data states render correctly
- current user row is identifiable
- no raw session content appears anywhere
