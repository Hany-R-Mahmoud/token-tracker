# Quickstart: Team Leaderboard Hardening And Rollout

Required checks:

- only intended aggregated fields are returned by leaderboard APIs
- opted-out users are hidden or anonymized correctly
- cross-team access is blocked
- setup docs are sufficient to configure GitHub OAuth and scheduled computation
- final docs clearly separate shipped behavior from future roadmap ideas
