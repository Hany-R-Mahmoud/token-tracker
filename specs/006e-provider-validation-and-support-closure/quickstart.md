# Quickstart: Provider Validation And Support Closure

Required checks:

- every priority provider has a current explicit status
- any newly claimed provider is validated on this machine
- unsupported/pending providers are still described honestly
- CLI/provider-status output matches docs and actual support
