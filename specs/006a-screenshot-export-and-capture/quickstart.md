# Quickstart: Screenshot Export And Capture

Required checks:

- export action produces an actual image or screenshot artifact
- exported output matches the intended surface
- export works without leaking raw session/prompt/code content
- existing clipboard text export still works if preserved
- docs describe screenshot export accurately
