# Quickstart: True Meters And Quota Sources

Required checks:

- every meter surface has an explicit semantic definition
- quota-backed states are only shown where source data is validated
- heuristic/fallback states are clearly labeled and non-misleading
- menubar, overview, and docs all describe the same semantics
