# Quickstart: Team Leaderboard Computation And API

Required checks:

- snapshot computation runs successfully
- composite score and tie-break rules match expected fixtures
- anonymous and opted-out states are handled correctly
- `today`, `week`, `month`, and `all_time` responses work
- manual recompute is admin-only and rate limited
- cross-team fetch is blocked
